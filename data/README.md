# Data directory

The primary input to this project is the live or local CLONEID database accessed through a read-only connection.

This directory is for optional derived artifacts only:

- `database_snapshots/`: frozen snapshots created for offline testing or reproducibility.
- `processed/`: derived intermediate data products generated by the workflow.

Do not treat manually prepared exports as the required input interface.
